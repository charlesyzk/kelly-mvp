# 高阶矩动态 Kelly 项目开发约定

## 当前边界

这是研究验证工具，不是实盘系统。当前冻结口径以 `README.md` 为对外说明，并以代码和测试为事实依据：

- 六模型：`M2_LOG`、`M3_LOG`、`M4_LOG_ZERO`、`M4_SIMPLE`、`M4_LOG_MEAN`、`EMPIRICAL_EXACT`；
- 日/周/月窗口 252/104/60，最低正式样本 252/52/24；
- 独立仓位 `RAW / BOUNDED / SAFE`，边界 `[-1,1]`，默认 κ=0.8；
- 不启用 Fractional/Half Kelly、TRADE 回退、70/30 切分或交易成本；
- 循环区块 Bootstrap 20/8/6、2000 次、种子 20260904；候选模型 × 仓位类型分别在 18 个标的频率组内做 BH-FDR；
- EODHD 与 Excel 可直接提供日/周/月；日线 CSV 聚合只作兼容和交叉核验。

## 不变量

- 严禁前视；每个标的、频率独立滚动并评价下一条完整收益。
- `RAW` 缺失保持缺失；`BOUNDED` 在边界内直接优化；`SAFE` 在边界与收敛安全域交集内优化，三者不得回填。
- κ 由用户预先选择并触发自动重算，不得按历史收益搜索最优 κ。
- 财富倍数为 `1+fR`；不大于零记录破产。统计量是 `log(max(1+fR,1e-12))`。
- 零仓位不进入方向准确率；非零仓位遇零收益为方向失败。
- 页面、CSV、XLSX、TXT 均来自同一后端计算，不写死结果。
- 每个标的、频率、模型、仓位类型独立生成模拟操作流水；无变化不成交易，RAW 标记为研究用途，缺失区间不跨越推测。
- `signals.csv` 和 `periods.csv` 保留十二个滚动矩、目标值及求解状态；`trades.csv` 只做目标仓位变化解释，不冒充券商成交。
- `pending` 没有下一期结果，不进入准确率或收益统计。
- EODHD Token 只能由 `EODHD_API_TOKEN` 注入，禁止进入源码、日志、报告、测试或提交。
- 新运行写入独立目录，不覆盖旧基线；不同配置不得冒充同一研究。

## 修改与验收

策略、数据、统计或输出契约改变时，同步源码、测试、`README.md` 和 `README_给朋友.md`。新行为至少有单元/边界测试和端到端契约测试。交付说明改动、未改项、数据配置、测试和限制。

```bash
PYTHONPATH=src python3 -m unittest discover -s tests -v
node --check src/kelly_mvp/web_static/app.js
```

研究结果不构成投资建议。没有完整成本、滑点、融资融券、税费及真实执行验证前，不得宣称可实盘。
