# K/60 项目开发约定

## 产品边界

这是一个最小、可审计的滚动四阶矩 Kelly 研究工具。核心用户不需要具备较强编程能力：上传日频 CSV、设置少量参数、运行并理解结果。

默认研究口径：

- 单一模型 `M4_SIMPLE`；
- 日、周、月分别使用最近 60 个同频率简单收益；
- 四阶目标 `q1*f - q2*f^2/2 + q3*f^3/3 - q4*f^4/4`；
- Full Kelly 在 `[-1, 1]` 内优化，默认再乘 `0.5`；
- 仓位只评价下一期收益，严禁前视；
- 重点报告方向准确率、策略收益和买入持有，同时保留回撤、覆盖率、换手及成本。

除非用户明确改变研究问题，不增加更多模型、自动调参平台、交易所接入、组合优化或复杂统计检验。

## 真实性与解释

- 网页指标必须来自后端逐期真实计算，禁止写死展示数字。
- `SYNTHETIC_DEMO` 只能用于工程演示，必须明确标注为合成数据。
- 私有、专有或需要预授权的行情不进入版本库。仅可提交明确允许再分发的测试数据，并保留来源、许可标签、引用文本与哈希。
- 汇总数字必须能从 `periods.csv` 逐行复算。
- “方向准确”“策略盈利”“跑赢买入持有”“可实盘”是不同命题，不混为一谈。
- 当前模型只假设最近 60 期的前四阶矩能代表下一期，并未拟合完整参数分布。
- 结果属于研究，不构成投资建议；未完整建模滑点、融资、融券、税费和执行限制时，不宣称可实盘。

## 数据与时间

- 输入统一转换为 `date,symbol,adjusted_close`；来源可以是用户 CSV 或服务端 EODHD 日线接口。价格必须有限且大于零，同一标的日期不得重复。
- EODHD Token 只能从服务端环境变量读取，不进入浏览器、日志、报告、源码或版本库；接口异常不得泄露含 Token 的 URL。
- 周/月由日线保守聚合；无法确认完整的最后周期应排除。
- 60 个收益需要至少 61 个价格，另需下一价格才能评价。
- 每个标的、频率独立计算，不能跨标的或跨频率拼接窗口。

## 修改与验收

- 参数修改先看 development，策略锁定后再看 holdout；看过 holdout 后继续修改，就不能继续称其为未见样本。
- 修改计算逻辑时必须同步网页中的“计算逻辑”说明。
- 修复缺陷先增加回归测试；新行为至少包含单元测试和端到端测试。
- 每次交付至少报告：改了什么、使用什么数据与配置、运行哪些测试、仍有哪些假设和限制。

测试命令：

```bash
PYTHONPATH=src python3 -m unittest discover -s tests -v
node --check src/kelly_mvp/web_static/app.js
```
